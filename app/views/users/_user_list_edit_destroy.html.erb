<div class="form-actions">
  <%= link_to("Display All Users", users_path, class: 'button radius')%>
  <%= link_to("Edit #{@user.name}", edit_user_path(@user), class: 'button radius') %>
  <%= link_to("Delete #{@user.name}", user_path(@user), method: 'delete', data: {confirm: "Are you sure you want to delete #{@user.name}?"}, class: 'button radius alert')%>
</div>
